{
  "name": "tape-taupe",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . tape-taupe --platform win32 --arch x64 --out dist/",
    "setup": "electron-installer-windows --src dist/tape-taupe-win32-x64/ --dest dist/installers/"
  },
  "devDependencies": {
    "electron": "^11.5.0",
    "electron-installer-windows": "^3.0.0",
    "electron-packager": "^15.2.0"
  }
}
